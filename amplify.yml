version: 1
frontend:
  phases:
    build:
      commands:
        - nvm install 18
        - nvm use 18
        - cd server
        - npm ci
  artifacts:
    baseDirectory: server # Required, even for backend-only, points to server directory
    files:
      - '**/*'